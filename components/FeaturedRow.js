import React from 'react'
import { View, Text } from 'react-native'

export const FeaturedRow = ({title, description, featuredCategory }) => {
  return (
    <View>
        <Text> Featured Row </Text>
    </View>
  )
}
